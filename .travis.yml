language: python
sudo: false
python:
    - 2.7
    - 3.6
    - 3.7-dev
    - pypy
    - pypy3
install:
    - pip install tox-travis coveralls coverage
script:
    - tox
after_success:
    - coveralls
cache:
  pip: true
